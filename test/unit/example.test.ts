import { expect, request, server  } from "../helpers";

describe("Group of Component", () => {

    describe("Component", () => {

        it("Test case", () => {
            expect(1).to.equal(1);
        });

    });

});
